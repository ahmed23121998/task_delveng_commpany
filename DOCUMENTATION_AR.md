# توثيق المشروع - Delveng Course Journey Map

## نظرة عامة على المشروع

هذا المشروع هو تطبيق ويب تفاعلي لتعليم الهندسة (MEP Engineering) مبني باستخدام **Next.js** و **React** و **TypeScript**. التطبيق يعرض تفاصيل دورة تدريبية مع خريطة تفاعلية للدروس تسمح للمستخدمين بالتنقل بين الدروس ومشاهدة تفاصيلها.

---

## 🏗️ البنية التقنية للمشروع

### التقنيات المستخدمة

1. **Next.js 16** - إطار عمل React للإنتاج
2. **React 19** - مكتبة JavaScript لبناء واجهات المستخدم
3. **TypeScript** - لغة برمجة توفر نوع البيانات الثابت
4. **Tailwind CSS** - إطار عمل CSS للتصميم
5. **Radix UI** - مكونات واجهة المستخدم القابلة للوصول
6. **Lucide React** - مكتبة الأيقونات

---

## 📁 هيكل المشروع

```
project/
├── app/                    # مجلد Next.js الرئيسي
│   ├── layout.tsx         # التخطيط الأساسي للتطبيق
│   ├── page.tsx           # الصفحة الرئيسية
│   └── globals.css        # الأنماط العامة
│
├── src/
│   ├── components/        # المكونات القابلة لإعادة الاستخدام
│   │   ├── LessonModal/   # نافذة منبثقة لعرض تفاصيل الدرس
│   │   └── ui/            # مكونات واجهة المستخدم الأساسية
│   │       ├── badge.tsx
│   │       ├── button.tsx
│   │       ├── card.tsx
│   │       └── ...
│   │
│   ├── screens/           # شاشات التطبيق الرئيسية
│   │   └── CourseDetails/ # شاشة تفاصيل الدورة
│   │       ├── CourseDetails.tsx
│   │       └── sections/  # أقسام الشاشة
│   │           ├── CourseDetailsSection/  # قسم تفاصيل الدورة
│   │           ├── LessonSection/         # قسم الدروس (Footer)
│   │           └── NavigationSection/      # قسم التنقل (Header)
│   │
│   └── lib/               # مكتبات مساعدة
│       └── utils.ts      # دوال مساعدة
│
├── public/                # الملفات الثابتة (صور، أيقونات)
├── package.json          # ملف التبعيات
├── tailwind.config.js    # إعدادات Tailwind CSS
└── next.config.js        # إعدادات Next.js
```

---

## 🔍 شرح المكونات الرئيسية

### 1. الصفحة الرئيسية (`app/page.tsx`)

```typescript
export default function Home() {
  return <CourseDetails />;
}
```

- **الوظيفة**: نقطة الدخول الرئيسية للتطبيق
- **المكون**: يستدعي مكون `CourseDetails` لعرض تفاصيل الدورة

---

### 2. التخطيط الأساسي (`app/layout.tsx`)

**الوظائف**:

- تحديد العنوان والوصف للتطبيق
- تحميل خطوط Google Fonts (Montserrat و Inter)
- توفير التخطيط الأساسي لجميع الصفحات

**الميزات**:

- Metadata للتطبيق (SEO)
- خطوط مخصصة للتصميم

---

### 3. قسم التنقل (`NavigationSection`)

**الموقع**: `src/screens/CourseDetails/sections/NavigationSection/NavigationSection.tsx`

**الوظائف**:

- شريط تنقل ثابت في أعلى الصفحة
- شعار الشركة
- حقل بحث للدورات
- قائمة التنقل (Home, Courses, About, Pricing, Careers)
- أزرار التسجيل والانضمام
- قائمة تنقل متجاوبة للهواتف المحمولة

**الميزات التفاعلية**:

- قائمة جانبية للهواتف المحمولة
- حالة نشطة للصفحة الحالية
- تأثيرات hover و transitions

---

### 4. قسم تفاصيل الدورة (`CourseDetailsSection`)

**الموقع**: `src/screens/CourseDetails/sections/CourseDetailsSection/CourseDetailsSection.tsx`

**الوظائف الرئيسية**:

#### أ. بطاقة معلومات الدورة

- صورة الدورة
- اسم الدورة (MEP Engineering)
- التقييم (5.0 نجوم)
- عدد الدروس (7 lessons)
- عدد التمارين (10 Practice)
- زر "Level Assessment"

#### ب. مناطق المعرفة (Knowledge Areas)

- قائمة بمناطق المعرفة المختلفة:
  - Air Distribution System
  - Equipment Sizing
  - Exhaust System
  - Piping System
  - System Type Selection
  - Thermal Analysis
  - Ventilation
  - Zoning and Control Strategy
- إمكانية التمرير الأفقي
- تمييز المنطقة النشطة

#### ج. خريطة الدروس التفاعلية

- **الوظيفة الأساسية**: عرض الدروس على خريطة تفاعلية
- **حالات الدروس**:
  - `completed` - مكتمل (أخضر)
  - `in_progress` - قيد التقدم (برتقالي)
  - `locked` - مقفل (رمادي)
  - `available` - متاح

**البيانات المخزنة**:

```typescript
const lessonsData = [
  {
    id: 0,
    name: "Foundation Lesson",
    status: "completed",
    description: "...",
    quizCount: 10,
    duration: "1 Hour",
    questionCount: 10,
  },
  // ... المزيد من الدروس
];
```

**الميزات التفاعلية**:

- النقر على درس لفتح نافذة منبثقة
- تأثيرات hover على الدروس
- مسارات بصرية تربط بين الدروس
- تأثيرات بصرية عند التمرير (opacity و drop-shadow)
- رسوم متحركة للدرس المحدد

#### د. بطاقة تفاصيل الدرس المحدد

- عرض معلومات الدرس المحدد
- عدد الاختبارات
- المدة الزمنية
- عدد الأسئلة
- زر "Start Lesson"

---

### 5. نافذة الدرس المنبثقة (`LessonModal`)

**الموقع**: `src/components/LessonModal/LessonModal.tsx`

**الوظائف**:

- عرض تفاصيل الدرس في نافذة منبثقة
- معلومات الدرس:
  - الاسم
  - الحالة (Locked, In Progress, Completed, Available)
  - الوصف
  - عدد الاختبارات
  - المدة الزمنية
  - عدد الأسئلة

**الأزرار حسب الحالة**:

- `completed` → "Review Lesson"
- `in_progress` → "Continue Lesson"
- `locked` → "Unlock Lesson" (معطل)
- `available` → "Start Lesson"

**الميزات**:

- خلفية شفافة عند الفتح
- إمكانية الإغلاق بالنقر خارج النافذة
- رسوم متحركة للفتح والإغلاق
- تصميم متجاوب

---

### 6. قسم الدروس (`LessonSection`)

**الموقع**: `src/screens/CourseDetails/sections/LessonSection/LessonSection.tsx`

**الوظيفة**: يعمل كـ Footer للتطبيق

**المحتوى**:

- شعار الشركة
- وصف الشركة
- روابط التنقل
- روابط وسائل التواصل الاجتماعي (Facebook, Instagram, Twitter, LinkedIn)
- حقوق النشر

---

### 7. المكون الرئيسي (`CourseDetails`)

**الموقع**: `src/screens/CourseDetails/CourseDetails.tsx`

**الوظيفة**: يجمع جميع الأقسام معاً

**الهيكل**:

1. `NavigationSection` - شريط التنقل
2. Breadcrumb - مسار التنقل (Home > Courses > MEP Engineering)
3. `CourseDetailsSection` - قسم التفاصيل الرئيسي
4. `LessonSection` - Footer

---

## 🎨 نظام التصميم

### الألوان المستخدمة

- **Primary Color**: `#ff7f24` (برتقالي)
- **Secondary Color**: ألوان ثانوية للتصميم
- **Black Shades**: درجات مختلفة من الأسود للخطوط
- **Background**: أبيض مع درجات رمادية فاتحة

### الخطوط

- **Montserrat**: للعناوين والأزرار
- **Inter**: للنصوص العادية

### التصميم المتجاوب

- **Mobile First**: التصميم يبدأ من الهواتف المحمولة
- **Breakpoints**: sm, md, lg, xl
- **Adaptive Layout**: التخطيط يتكيف مع حجم الشاشة

---

## 🔄 تدفق البيانات والتفاعل

### حالة المكونات (State Management)

```typescript
// في CourseDetailsSection
const [selectedLesson, setSelectedLesson] = useState(null);
const [hoveredLessonId, setHoveredLessonId] = useState(null);
```

**التدفق**:

1. المستخدم ينقر على درس في الخريطة
2. يتم تحديث `selectedLesson`
3. تفتح `LessonModal` مع بيانات الدرس
4. المستخدم يمكنه إغلاق النافذة أو بدء الدرس

### معالجة الأحداث

- **onClick**: النقر على درس
- **onMouseEnter/onMouseLeave**: تأثيرات hover
- **onClose**: إغلاق النافذة المنبثقة

---

## 🚀 كيفية تشغيل المشروع

### المتطلبات الأساسية

- Node.js (الإصدار 18 أو أحدث)
- npm أو yarn

### خطوات التشغيل

1. **تثبيت التبعيات**:

```bash
npm install
```

2. **تشغيل المشروع في وضع التطوير**:

```bash
npm run dev
```

3. **فتح المتصفح**:
   افتح [http://localhost:3000](http://localhost:3000)

4. **بناء المشروع للإنتاج**:

```bash
npm run build
```

5. **تشغيل النسخة المبنية**:

```bash
npm start
```

---

## 📦 المكونات القابلة لإعادة الاستخدام (UI Components)

### المكونات من shadcn/ui

1. **Button** - أزرار قابلة للتخصيص
2. **Card** - بطاقات لعرض المحتوى
3. **Badge** - شارات للتصنيفات
4. **ScrollArea** - منطقة تمرير مخصصة
5. **Separator** - فاصل بصري
6. **Input** - حقول الإدخال
7. **Select** - قوائم منسدلة

---

## 🎯 الميزات الرئيسية

### 1. خريطة الدروس التفاعلية

- عرض بصري للدروس وعلاقاتها
- مسارات بصرية تربط بين الدروس
- تأثيرات تفاعلية عند التمرير والنقر

### 2. نظام حالات الدروس

- تتبع تقدم المستخدم
- منع الوصول للدروس المقفلة
- تمييز بصري للحالات المختلفة

### 3. تصميم متجاوب

- يعمل على جميع الأجهزة
- قائمة تنقل متجاوبة للهواتف
- تخطيط مرن يتكيف مع الشاشة

### 4. تجربة مستخدم محسنة

- رسوم متحركة سلسة
- تأثيرات hover
- نافذة منبثقة تفاعلية
- تصميم حديث وجذاب

---

## 🔧 الإعدادات والتكوين

### Next.js Config (`next.config.js`)

```javascript
{
  output: 'export',  // تصدير ثابت للموقع
  images: {
    unoptimized: true,  // تعطيل تحسين الصور
  }
}
```

### Tailwind Config (`tailwind.config.js`)

- ألوان مخصصة من CSS Variables
- خطوط مخصصة
- رسوم متحركة مخصصة (pulse-glow)
- إعدادات responsive

---

## 📝 ملاحظات مهمة للانترفيو

### نقاط القوة في المشروع

1. **بنية منظمة**: فصل واضح بين المكونات
2. **TypeScript**: نوع البيانات الثابت يقلل الأخطاء
3. **تصميم متجاوب**: يعمل على جميع الأجهزة
4. **مكونات قابلة لإعادة الاستخدام**: سهولة الصيانة
5. **تجربة مستخدم ممتازة**: تفاعلات سلسة ورسوم متحركة

### التقنيات المستخدمة

- **Next.js**: لإنتاج تطبيقات React محسنة
- **TypeScript**: للأمان في البرمجة
- **Tailwind CSS**: للتصميم السريع
- **Radix UI**: لمكونات واجهة المستخدم القابلة للوصول

### التحديات المحتملة والحلول

1. **إدارة الحالة**: استخدام useState محلي (يمكن الترقية إلى Context API أو Zustand)
2. **البيانات**: حالياً البيانات ثابتة (يمكن ربطها بـ API)
3. **الأداء**: استخدام Next.js للتحسين التلقائي

---

## 🎓 شرح سريع للكود

### مثال: كيفية عمل النقر على درس

```typescript
// 1. عند النقر على درس
const handleLessonClick = (lessonId: number) => {
  const lesson = lessonsData.find((l) => l.id === lessonId);
  if (lesson && lesson.status !== "locked") {
    setSelectedLesson(lesson); // تحديث الحالة
  }
};

// 2. المكون LessonModal يستقبل البيانات
<LessonModal
  isOpen={!!selectedLesson} // يفتح إذا كان هناك درس محدد
  onClose={handleCloseModal}
  lesson={selectedLesson} // بيانات الدرس
/>;

// 3. في LessonModal
if (!isOpen || !lesson) return null; // لا يعرض إذا كان مغلق
```

---

## 📚 موارد إضافية

- [Next.js Documentation](https://nextjs.org/docs)
- [React Documentation](https://react.dev)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [Radix UI](https://www.radix-ui.com)
- [TypeScript](https://www.typescriptlang.org/docs)

---

## ✅ الخلاصة

هذا المشروع هو تطبيق تعليمي تفاعلي يعرض:

- تفاصيل دورة تدريبية
- خريطة تفاعلية للدروس
- نظام تتبع التقدم
- واجهة مستخدم حديثة ومتجاوبة

المشروع مبني بأفضل الممارسات في تطوير React/Next.js ويستخدم TypeScript للأمان والجودة.

---

**تاريخ الإنشاء**: 2025  
**الإصدار**: 1.0.0  
**المطور**: Delveng Team
